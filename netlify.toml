[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA routing: all routes serve index.html
# The frontend's global fetch/axios interceptor routes /api/* to Azure VM directly.
# No server-side proxying needed here.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
